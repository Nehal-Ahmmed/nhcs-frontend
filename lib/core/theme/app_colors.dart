import 'package:flutter/material.dart';

class AppColors {
  // Primary (Premium Trust Medical Blue)
  static const Color primary = Color(0xFF2563EB); // Royal Blue
  static const Color primaryDark = Color(0xFF1D4ED8); // Deep Blue
  static const Color primaryLight = Color(0xFFDBEAFE); // Soft Light Blue

  // Secondary (Sky / Active Blue)
  static const Color secondary = Color(0xFF3B82F6); // Standard Sky Blue
  static const Color secondaryLight = Color(0xFFEFF6FF); // Ultra Soft Blue

  // Accent (Clinical Red / Emergency Cross)
  static const Color accent = Color(0xFFDC2626); // Strong Medical Red
  static const Color accentLight = Color(0xFFFEE2E2); // Soft Red Tint

  // Surfaces (Clean Slate / Premium Navy Dark Mode)
  static const Color surface = Color(0xFFFFFFFF);
  static const Color background = Color(
    0xFFF8FAFC,
  ); // Very light grey-blue clinical background
  static const Color sidebar = Color(0xFF0F172A); // Premium dark slate sidebar
  static const Color sidebarHover = Color(0xFF1E293B); // Slate hover tint

  // Status
  static const Color success = Color(0xFF22C55E);
  static const Color successLight = Color(0xFFDCFCE7);
  static const Color warning = Color(0xFFF59E0B);
  static const Color warningLight = Color(0xFFFEF3C7);
  static const Color danger = Color(0xFFEF4444);
  static const Color dangerLight = Color(0xFFFEE2E2);
  static const Color info = Color(0xFF3B82F6);
  static const Color infoLight = Color(0xFFDBEAFE);

  // Text
  static const Color textPrimary = Color(0xFF0F172A); // Near Black
  static const Color textSecondary = Color(0xFF475569); // Dark Grey
  static const Color textMuted = Color(0xFF94A3B8); // Muted Slate
  static const Color textOnPrimary = Color(0xFFFFFFFF);

  // Borders & Dividers
  static const Color divider = Color(0xFFE2E8F0);
  static const Color border = Color(0xFFCBD5E1);
}
